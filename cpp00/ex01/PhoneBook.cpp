#include "Phonebook.hpp"

int main()
{
	PhoneBook rec;
	std::string	str;

	while (1)
	{
		getline(std::cin, str);
		if (str == "EXIT")
			break ;
		if (str == "ADD")
		{
			std::cin;
		}
	}
	return 0;
}